{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run start.sh",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/controller/app.py",
            "console": "integratedTerminal",
            "preLaunchTask": "run start",
            "python": "${workspaceFolder}/env/bin/python",
            "cwd": "${workspaceFolder}"
        }
        
    ]
}
